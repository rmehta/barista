i want you to create detailed specs for a new frappe product called "barista". the purpose of this product is to help people manage their local frappe sites with a frappe-ui based front-end. the app should be able to create and manage benches, install apps, create and manage sites, backups, view system performance, background jobs, error logs. it should show web analytics, database analytics, slow query logs, bin log browsing. the creation of benches should be based on docker containers. once barista is installed, it should create one bench and add a "barista" site on it which will contain the state of the current bench. the user can use the same bench or create more benches. take inspiration from frappe/press. create a spec and add it to the "barista folder". the spec should use as much as standard Frappe API and concepts. add some screenshots or wireframes for the front end based on frappe-ui. also create a separate install script for barista so that it is easy to install on a self hosted server.
